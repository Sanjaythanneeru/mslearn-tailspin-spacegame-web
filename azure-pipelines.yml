trigger:
- main
pool:
  name: 'Default'

variables:
  buildConfiguration: 'Release'

steps:
- script: dotnet build --configuartion $(buildConfiguartion)
  displayName: 'dotnet build $(buildConfiguration'